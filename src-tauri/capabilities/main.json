{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "main-window",
  "description": "Permissions for the main app window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default",
    "store:allow-get",
    "store:allow-set",
    "store:allow-save"
  ]
}
